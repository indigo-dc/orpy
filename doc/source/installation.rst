.. _installation:

============
Installation
============

At the command line::

    $ pip install orpy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv orpy
    $ pip install orpy
